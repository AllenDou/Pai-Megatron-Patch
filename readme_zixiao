
如果是断点续训, load ckpt时会报错, 需要修改 PAI-Megatron-LM-240718/megatron/training/checkpointing.py 607行 
-        state_dict = torch.load(checkpoint_name, map_location='cpu')
+        state_dict = torch.load(checkpoint_name, map_location='cpu', weights_only=False)

PAI-Megatron-LM-240718 是@同润维护的megatron-lm

同时删除 b/examples/qwen2_5/run_mcore_qwen.sh的 
--no-load-optim 和 --no-save-optim

数据处理见
https://github.com/alibaba/Pai-Megatron-Patch/blob/main/toolkits/pretrain_data_preprocessing/README.md

       convert          pretrain/sft
hf_ckpt ---> megatron_ckpt ---> megatron_ckpt ---> hf_ckpt ---> vllm

megatron的pretrain/sft只是加速训练, 其实还需要hf的tokenizer.json等多个文件, 所以 pretrain会有cp json等动作.
